# Netlify proxy: browser calls https://bornlottery.chingyu.site/api/*
# Netlify fetches from the EB backend server-side (avoids HTTPS->HTTP mixed content)
/api/*  http://born-lottery.ap-southeast-2.elasticbeanstalk.com/api/:splat  200
