# Local / OS / Python
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env
.env.*
.dockerignore
!.env.example
!.dockerignore.example

# Resolve logs
data/wiki_resolve*.log

# Build tooling
scripts/__pycache__/

# Full art library & masters — keep local / S3 only (not GitHub, not Netlify)
assests/icons/
assests/flags/
assests/original_images/
private/original_images/
private/icons/
private/flags/

# Optional local notes
*.pem
*.key

# Personal project overview (for resume drafting — keep local, not public)
docs/overview_bornlottery.md
